{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the single main window. The shell asks for nothing beyond Tauri's own defaults: the client talks to the server over HTTP and a WebSocket like any browser would, so no filesystem, shell or process permission is needed here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
